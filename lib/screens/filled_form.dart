import 'package:flutter/material.dart';

class FilledForm extends StatelessWidget {
  const FilledForm({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
